#pragma once

namespace domain {

/** Must stay in sync with `EliteHybridBehavior` melee branch in `enemy_behavior.cpp`. */
inline constexpr float kEliteHybridMeleeManhattanWorld = 4.f;

} // namespace domain
